import { useEffect, useMemo, useState } from "react";
import {
  motion,
  AnimatePresence,
} from "framer-motion";

import {
  BrainCircuit,
  Sparkles,
  GraduationCap,
  BarChart3,
  History,
  Volume2,
  VolumeX,
  ChevronRight,
  ArrowLeft,
  Trophy,
  Activity,
} from "lucide-react";

export default function App() {

  // ==================================================
  // STATE
  // ==================================================

  const [page, setPage] =
    useState("intro");

  const [shiftDate, setShiftDate] =
    useState("");

  const [session, setSession] =
    useState("");

  const [category, setCategory] =
    useState("OPEN");

  const [physics, setPhysics] =
    useState("");

  const [chemistry, setChemistry] =
    useState("");

  const [maths, setMaths] =
    useState("");

  const [sound, setSound] =
    useState(true);

  const [percentile, setPercentile] =
    useState("0.00");

  const [history, setHistory] =
    useState([]);

  const [loadingText, setLoadingText] =
    useState(
      "Analyzing normalization trends..."
    );

  // ==================================================
  // SOUND
  // ==================================================

  const playSound = () => {

    if (!sound) return;

    const audio = new Audio(
      "https://assets.mixkit.co/active_storage/sfx/2571/2571-preview.mp3"
    );

    audio.volume = 0.12;

    audio.play();
  };

  // ==================================================
  // SHIFT DATA
  // ==================================================

  const shifts = [

    {
      date: "11 Apr 2026",

      morning: {
        difficulty: "Easy",
        above120: 8.85,
        highest: 183,
        students: 949,
      },

      evening: {
        difficulty: "Hard",
        above120: 4.78,
        highest: 182,
        students: 878,
      },
    },

    {
      date: "13 Apr 2026",

      morning: {
        difficulty: "Moderate",
        above120: 6.41,
        highest: 174,
        students: 983,
      },

      evening: {
        difficulty: "Hard",
        above120: 5.20,
        highest: 170,
        students: 1019,
      },
    },

    {
      date: "15 Apr 2026",

      morning: {
        difficulty: "Hard",
        above120: 7.00,
        highest: 173,
        students: 1029,
      },

      evening: {
        difficulty: "Moderate",
        above120: 7.38,
        highest: 162,
        students: 948,
      },
    },

    {
      date: "16 Apr 2026",

      morning: {
        difficulty: "Moderate",
        above120: 6.19,
        highest: 176,
        students: 856,
      },

      evening: {
        difficulty: "Hard",
        above120: 5.61,
        highest: 196,
        students: 767,
      },
    },

    {
      date: "17 Apr 2026",

      morning: {
        difficulty: "Hard",
        above120: 5.22,
        highest: 162,
        students: 728,
      },

      evening: {
        difficulty: "Moderate",
        above120: 5.76,
        highest: 194,
        students: 665,
      },
    },

    {
      date: "18 Apr 2026",

      morning: {
        difficulty: "Moderate",
        above120: 5.50,
        highest: 177,
        students: 491,
      },

      evening: {
        difficulty: "Moderate",
        above120: 5.11,
        highest: 171,
        students: 540,
      },
    },

    {
      date: "19 Apr 2026",

      morning: {
        difficulty: "Hard",
        above120: 4.19,
        highest: 151,
        students: 453,
      },

      evening: {
        difficulty: "Hard",
        above120: 4.44,
        highest: 158,
        students: 428,
      },
    },

    {
      date: "20 Apr 2026",

      morning: {
        difficulty: "Moderate",
        above120: 4.62,
        highest: 164,
        students: 303,
      },

      evening: {
        difficulty: "Hard",
        above120: 4.81,
        highest: 169,
        students: 288,
      },
    },
  ];

  // ==================================================
  // SHIFT SELECTED
  // ==================================================

  const selectedShift = shifts.find(
    (s) => s.date === shiftDate
  );

  const shiftData =
    session === "Morning"
      ? selectedShift?.morning
      : selectedShift?.evening;

  // ==================================================
  // VALIDATION
  // ==================================================

  const handleMarks = (
    value,
    setter,
    max
  ) => {

    if (value === "") {
      setter("");
      return;
    }

    let num = Number(value);

    if (num < 0) num = 0;

    if (num > max) num = max;

    setter(num);
  };

  // ==================================================
  // TOTAL
  // ==================================================

  const totalMarks =
    Number(physics || 0) +
    Number(chemistry || 0) +
    Number(maths || 0);

  // ==================================================
  // PERCENTILE ENGINE
  // ==================================================

  const calculatePercentile = () => {

    if (!shiftData) return 0;

    const diff =
      shiftData.difficulty;

    let base = 0;

    // HARD

    if (diff === "Hard") {

      if (totalMarks >= 150)
        base =
          99.86 +
          (totalMarks - 150) *
            0.006;

      else if (totalMarks >= 140)
        base =
          99.55 +
          (totalMarks - 140) *
            0.031;

      else if (totalMarks >= 130)
        base =
          99.15 +
          (totalMarks - 130) *
            0.04;

      else if (totalMarks >= 120)
        base =
          98.70 +
          (totalMarks - 120) *
            0.045;

      else if (totalMarks >= 110)
        base =
          97.80 +
          (totalMarks - 110) *
            0.09;

      else if (totalMarks >= 100)
        base =
          96.60 +
          (totalMarks - 100) *
            0.12;

      else if (totalMarks >= 90)
        base =
          94.90 +
          (totalMarks - 90) *
            0.17;

      else
        base =
          80 +
          totalMarks * 0.16;
    }

    // MODERATE

    else if (
      diff === "Moderate"
    ) {

      if (totalMarks >= 150)
        base =
          99.76 +
          (totalMarks - 150) *
            0.005;

      else if (totalMarks >= 140)
        base =
          99.42 +
          (totalMarks - 140) *
            0.034;

      else if (totalMarks >= 130)
        base =
          98.98 +
          (totalMarks - 130) *
            0.044;

      else if (totalMarks >= 120)
        base =
          98.42 +
          (totalMarks - 120) *
            0.056;

      else if (totalMarks >= 110)
        base =
          97.45 +
          (totalMarks - 110) *
            0.097;

      else if (totalMarks >= 100)
        base =
          96.20 +
          (totalMarks - 100) *
            0.125;

      else if (totalMarks >= 90)
        base =
          94.50 +
          (totalMarks - 90) *
            0.17;

      else
        base =
          79 +
          totalMarks * 0.17;
    }

    // EASY

    else {

      if (totalMarks >= 150)
        base =
          99.68 +
          (totalMarks - 150) *
            0.004;

      else if (totalMarks >= 140)
        base =
          99.30 +
          (totalMarks - 140) *
            0.038;

      else if (totalMarks >= 130)
        base =
          98.78 +
          (totalMarks - 130) *
            0.052;

      else if (totalMarks >= 120)
        base =
          98.05 +
          (totalMarks - 120) *
            0.073;

      else if (totalMarks >= 110)
        base =
          97.00 +
          (totalMarks - 110) *
            0.105;

      else if (totalMarks >= 100)
        base =
          95.60 +
          (totalMarks - 100) *
            0.14;

      else if (totalMarks >= 90)
        base =
          93.60 +
          (totalMarks - 90) *
            0.20;

      else
        base =
          78 +
          totalMarks * 0.17;
    }

    // CATEGORY BOOST

    if (
      category === "SC" ||
      category === "ST"
    )
      base += 0.25;

    if (
      category === "VJNT"
    )
      base += 0.12;

    // DENSITY CORRECTION

    if (shiftData.above120 < 5)
      base += 0.15;

    return Math.min(base, 99.99)
      .toFixed(2);
  };

  // ==================================================
  // RANK
  // ==================================================

  const getRank = () => {

    const p =
      Number(percentile);

    if (p >= 99.8)
      return "Under 500";

    if (p >= 99.5)
      return "500 - 1.5k";

    if (p >= 99)
      return "1.5k - 3k";

    if (p >= 98)
      return "3k - 6k";

    if (p >= 97)
      return "6k - 10k";

    if (p >= 95)
      return "10k - 18k";

    return "18k+";
  };

  // ==================================================
  // QUOTES
  // ==================================================

  const getQuote = () => {

    const p =
      Number(percentile);

    if (p >= 99)
      return "You survived Maharashtra’s academic battle royale.";

    if (p >= 97)
      return "Not perfection. Still dangerous.";

    if (p >= 95)
      return "Consistency beats panic.";

    return "One exam never gets the final vote.";
  };

  // ==================================================
  // COLLEGES
  // ==================================================

  const colleges = useMemo(() => {

    const p =
      Number(percentile);

    if (p >= 99)
      return [

        {
          tier: "Dream",
          name: "COEP Pune",
          branch: "ENTC",
          cutoff: "99.3+",
        },

        {
          tier: "Dream",
          name: "VJTI Mumbai",
          branch: "Mechanical",
          cutoff: "99.4+",
        },

        {
          tier: "Strong",
          name: "PICT Pune",
          branch: "IT",
          cutoff: "99+",
        },

        {
          tier: "Strong",
          name: "WCE Sangli",
          branch: "ENTC",
          cutoff: "98.8+",
        },
      ];

    if (p >= 98)
      return [

        {
          tier: "Strong",
          name: "WCE Sangli",
          branch: "ENTC",
          cutoff: "98+",
        },

        {
          tier: "Strong",
          name: "VIT Pune",
          branch: "CS",
          cutoff: "97.8+",
        },

        {
          tier: "Safe",
          name: "PCCOE",
          branch: "CS",
          cutoff: "97+",
        },

        {
          tier: "Safe",
          name: "Walchand",
          branch: "Mechanical",
          cutoff: "96+",
        },
      ];

    return [

      {
        tier: "Safe",
        name: "Walchand",
        branch: "Mechanical",
        cutoff: "95+",
      },

      {
        tier: "Safe",
        name: "VIT Pune",
        branch: "ENTC",
        cutoff: "95+",
      },

      {
        tier: "Safe",
        name: "PCCOE",
        branch: "Mechanical",
        cutoff: "94+",
      },
    ];
  }, [percentile]);

  // ==================================================
  // HISTORY
  // ==================================================

  useEffect(() => {

    const saved =
      JSON.parse(
        localStorage.getItem(
          "rankpulse-history"
        )
      ) || [];

    setHistory(saved);

  }, []);

  const saveHistory = (
    finalPercentile
  ) => {

    const data = {

      percentile:
        finalPercentile,

      marks: totalMarks,

      shift:
        shiftDate +
        " " +
        session,

      time:
        new Date().toLocaleString(),
    };

    const updated = [
      data,
      ...history,
    ].slice(0, 5);

    localStorage.setItem(
      "rankpulse-history",
      JSON.stringify(updated)
    );

    setHistory(updated);
  };

  // ==================================================
  // PREDICT
  // ==================================================

  const handlePredict = () => {

    playSound();

    setPage("loading");

    const texts = [

      "Matching 2025 normalization data...",

      "Analyzing percentile density...",

      "Comparing difficulty patterns...",

      "Generating rank estimates...",
    ];

    let i = 0;

    const textInterval =
      setInterval(() => {

        setLoadingText(
          texts[i % texts.length]
        );

        i++;

      }, 1100);

    const target =
      Number(
        calculatePercentile()
      );

    let current = 0;

    const interval =
      setInterval(() => {

        current += 0.13;

        if (current >= target) {

          current = target;

          clearInterval(
            interval
          );

          clearInterval(
            textInterval
          );

          saveHistory(
            target.toFixed(2)
          );

          setTimeout(() => {

            setPage("result");

          }, 500);
        }

        setPercentile(
          current.toFixed(2)
        );

      }, 8);
  };

  // ==================================================
  // GLASS
  // ==================================================

  const glass =
    "bg-white/10 backdrop-blur-3xl border border-white/10 rounded-[34px] shadow-[0_10px_60px_rgba(255,255,255,0.05)]";

  return (

    <div className="min-h-screen bg-[#07040d] overflow-hidden text-white relative">

      {/* BG */}

      <div className="absolute inset-0 overflow-hidden">

        <div className="absolute top-[-250px] left-[-200px] w-[650px] h-[650px] bg-pink-500/20 rounded-full blur-3xl animate-pulse" />

        <div className="absolute bottom-[-250px] right-[-200px] w-[650px] h-[650px] bg-violet-500/20 rounded-full blur-3xl animate-pulse" />

        <div className="absolute top-[40%] left-[40%] w-[300px] h-[300px] bg-cyan-400/10 rounded-full blur-3xl animate-bounce" />

      </div>

      {/* PARTICLES */}

      {[...Array(40)].map(
        (_, i) => (

          <div
            key={i}
            className="absolute bg-white/10 rounded-full animate-pulse"
            style={{
              width:
                Math.random() * 5 +
                2,
              height:
                Math.random() * 5 +
                2,
              left:
                Math.random() * 100 +
                "%",
              top:
                Math.random() * 100 +
                "%",
            }}
          />

        )
      )}

      <div className="relative z-10 max-w-7xl mx-auto px-5 py-10">

        {/* NAVBAR */}

        <div className={glass + " px-6 py-4 flex items-center justify-between sticky top-5 z-50"}>

          <div className="flex items-center gap-3">

            <BrainCircuit className="text-pink-300" />

            <div className="font-black text-2xl">

              RankPulse AI

            </div>

          </div>

          <div className="flex items-center gap-5">

            <button
              onClick={() =>
                setPage("intro")
              }
              className="text-sm text-pink-100/60 hover:text-white transition-all"
            >
              Home
            </button>

            <button
              onClick={() =>
                setPage("history")
              }
              className="text-sm text-pink-100/60 hover:text-white transition-all"
            >
              History
            </button>

            <button
              onClick={() =>
                setSound(!sound)
              }
            >

              {sound ? (
                <Volume2 />
              ) : (
                <VolumeX />
              )}

            </button>

          </div>

        </div>

        {/* INTRO */}

        {page === "intro" && (

          <div className="min-h-[85vh] flex flex-col items-center justify-center text-center">

            <motion.div
              animate={{
                rotate: [0, 5, -5, 0],
              }}
              transition={{
                repeat: Infinity,
                duration: 6,
              }}
            >

              <BrainCircuit
                size={90}
                className="text-pink-300"
              />

            </motion.div>

            <h1 className="text-7xl md:text-9xl font-black mt-10 bg-gradient-to-r from-pink-200 via-violet-200 to-cyan-200 bg-clip-text text-transparent">

              RankPulse AI

            </h1>

            <p className="mt-5 text-pink-100/50 tracking-[0.3em] uppercase text-sm">

              MHT CET 2026 Predictor

            </p>

            <button
              onClick={() => {
                playSound();
                setPage("shift");
              }}
              className="mt-14 px-10 py-5 rounded-full bg-gradient-to-r from-pink-300 to-violet-300 text-black font-black hover:scale-105 transition-all flex items-center gap-3"
            >

              Begin Analysis
              <ChevronRight />

            </button>

          </div>

        )}

        {/* SHIFT */}

        {page === "shift" && (

          <div className={glass + " p-10"}>

            <h2 className="text-5xl font-black mb-10">

              Select Shift

            </h2>

            <div className="grid md:grid-cols-4 gap-4">

              {shifts.map((s) => (

                <motion.button
                  whileHover={{
                    y: -4,
                    scale: 1.02,
                  }}
                  key={s.date}
                  onClick={() => {
                    playSound();
                    setShiftDate(
                      s.date
                    );
                    setPage(
                      "session"
                    );
                  }}
                  className="p-5 rounded-[28px] border border-white/10 bg-gradient-to-br from-pink-300/10 to-violet-300/10 text-left"
                >

                  <div className="text-xl font-black">

                    {s.date}

                  </div>

                  <div className="mt-4 flex gap-2 flex-wrap">

                    <span className="text-xs px-3 py-1 rounded-full bg-pink-400/20">

                      Morning:
                      {" "}
                      {
                        s.morning
                          .difficulty
                      }

                    </span>

                    <span className="text-xs px-3 py-1 rounded-full bg-violet-400/20">

                      Evening:
                      {" "}
                      {
                        s.evening
                          .difficulty
                      }

                    </span>

                  </div>

                </motion.button>

              ))}

            </div>

          </div>

        )}

        {/* SESSION */}

        {page === "session" && (

          <div className={glass + " p-12"}>

            <button
              onClick={() =>
                setPage("shift")
              }
              className="mb-8"
            >

              <ArrowLeft />

            </button>

            <h2 className="text-5xl font-black mb-10">

              Select Session

            </h2>

            <div className="grid md:grid-cols-2 gap-7">

              {[
                "Morning",
                "Evening",
              ].map((s) => (

                <button
                  key={s}
                  onClick={() => {
                    playSound();
                    setSession(s);
                    setPage(
                      "category"
                    );
                  }}
                  className="p-14 rounded-[32px] border border-white/10 bg-gradient-to-br from-pink-300/10 to-violet-300/10 hover:scale-[1.02] transition-all"
                >

                  <div className="text-5xl">

                    {s === "Morning"
                      ? "🌤"
                      : "🌙"}

                  </div>

                  <div className="text-3xl font-black mt-5">

                    {s}

                  </div>

                </button>

              ))}

            </div>

          </div>

        )}

        {/* CATEGORY */}

        {page === "category" && (

          <div className={glass + " p-12"}>

            <button
              onClick={() =>
                setPage(
                  "session"
                )
              }
              className="mb-8"
            >

              <ArrowLeft />

            </button>

            <h2 className="text-5xl font-black mb-10">

              Select Category

            </h2>

            <div className="flex flex-wrap gap-5">

              {[
                "OPEN",
                "OBC",
                "EWS",
                "VJNT",
                "SC",
                "ST",
              ].map((c) => (

                <button
                  key={c}
                  onClick={() => {
                    playSound();
                    setCategory(c);
                    setPage(
                      "marks"
                    );
                  }}
                  className="px-8 py-4 rounded-full bg-gradient-to-r from-pink-300/20 to-violet-300/20 border border-white/10 hover:scale-105 transition-all"
                >

                  {c}

                </button>

              ))}

            </div>

          </div>

        )}

        {/* MARKS */}

        {page === "marks" && (

          <div className={glass + " p-12"}>

            <button
              onClick={() =>
                setPage(
                  "category"
                )
              }
              className="mb-8"
            >

              <ArrowLeft />

            </button>

            <h2 className="text-5xl font-black mb-12">

              Enter Marks

            </h2>

            <div className="grid md:grid-cols-3 gap-7">

              {[
                [
                  "Physics",
                  physics,
                  setPhysics,
                  50,
                ],

                [
                  "Chemistry",
                  chemistry,
                  setChemistry,
                  50,
                ],

                [
                  "Mathematics",
                  maths,
                  setMaths,
                  100,
                ],
              ].map((item) => (

                <motion.div
                  whileHover={{
                    y: -4,
                  }}
                  key={item[0]}
                  className="p-8 rounded-[34px] border border-white/10 bg-gradient-to-br from-pink-300/10 to-violet-300/10"
                >

                  <div className="text-pink-100/60">

                    {item[0]}

                  </div>

                  <input
                    type="number"
                    value={item[1]}
                    onChange={(e) =>
                      handleMarks(
                        e.target
                          .value,
                        item[2],
                        item[3]
                      )
                    }
                    className="bg-transparent outline-none text-7xl font-black mt-5 w-full"
                  />

                  <div className="text-xs text-pink-100/40 mt-4">

                    Max:
                    {" "}
                    {item[3]}

                  </div>

                </motion.div>

              ))}

            </div>

            <div className="mt-14 text-center">

              <div className="uppercase tracking-[0.3em] text-pink-100/40 text-xs">

                Total Marks

              </div>

              <div className="text-[120px] font-black bg-gradient-to-r from-pink-100 to-cyan-100 bg-clip-text text-transparent">

                {totalMarks}

              </div>

            </div>

            <button
              onClick={
                handlePredict
              }
              className="w-full mt-10 rounded-[34px] bg-gradient-to-r from-pink-300 via-violet-300 to-cyan-300 text-black font-black text-2xl py-5 hover:scale-[1.01] transition-all"
            >

              Predict Percentile

            </button>

          </div>

        )}

        {/* LOADING */}

        {page === "loading" && (

          <div className={glass + " p-24 text-center"}>

            <div className="w-32 h-32 mx-auto border-4 border-pink-300 border-t-transparent rounded-full animate-spin" />

            <div className="mt-10 text-2xl text-pink-100/60">

              {loadingText}

            </div>

          </div>

        )}

        {/* RESULT */}

        {page === "result" && (

          <div>

            <div className={glass + " p-16 text-center"}>

              <div className="uppercase tracking-[0.4em] text-pink-100/40 text-xs">

                Predicted Percentile

              </div>

              <div className="text-[150px] font-black bg-gradient-to-b from-white via-pink-100 to-cyan-100 bg-clip-text text-transparent">

                {percentile}

              </div>

              <div className="text-2xl text-pink-100/60">

                percentile

              </div>

              <div className="mt-8 italic text-pink-100/70">

                "{getQuote()}"

              </div>

            </div>

            {/* STATS */}

            <div className="grid md:grid-cols-4 gap-6 mt-10">

              {[
                [
                  "Difficulty",
                  shiftData
                    ?.difficulty,
                ],

                [
                  "Estimated Rank",
                  getRank(),
                ],

                [
                  "120+ Students",
                  shiftData
                    ?.above120 +
                    "%",
                ],

                [
                  "Highest",
                  shiftData
                    ?.highest,
                ],
              ].map((item) => (

                <div
                  key={item[0]}
                  className={glass + " p-8"}
                >

                  <div className="text-pink-100/40 text-sm">

                    {item[0]}

                  </div>

                  <div className="text-4xl font-black mt-4">

                    {item[1]}

                  </div>

                </div>

              ))}

            </div>

            {/* GRAPH */}

            <div className={glass + " mt-10 p-10"}>

              <div className="flex items-center gap-3 text-3xl font-black mb-10">

                <BarChart3 />
                Marks Distribution

              </div>

              <div className="flex items-end gap-3 h-[260px]">

                {[22, 34, 55, 75, 95, 80, 60, 35, 15].map(
                  (
                    h,
                    i
                  ) => (

                    <div
                      key={i}
                      className="flex-1 rounded-t-3xl bg-gradient-to-t from-pink-400 to-violet-400 animate-pulse relative"
                      style={{
                        height:
                          h + "%",
                      }}
                    >

                      {i === 5 && (

                        <div className="absolute -top-8 left-1/2 -translate-x-1/2 text-xs">

                          You

                        </div>

                      )}

                    </div>

                  )
                )}

              </div>

            </div>

            {/* COLLEGES */}

            <div className={glass + " mt-10 p-10"}>

              <div className="flex items-center gap-3 text-3xl font-black mb-10">

                <GraduationCap />
                Predicted Colleges

              </div>

              <div className="grid md:grid-cols-2 gap-6">

                {colleges.map(
                  (
                    c,
                    i
                  ) => (

                    <div
                      key={i}
                      className="p-8 rounded-[30px] border border-white/10 bg-gradient-to-br from-pink-300/10 to-violet-300/10"
                    >

                      <div className="flex justify-between items-center">

                        <div className="text-2xl font-black">

                          {c.name}

                        </div>

                        <span className="text-xs px-3 py-1 rounded-full bg-pink-400/20">

                          {c.tier}

                        </span>

                      </div>

                      <div className="mt-5 text-pink-100/70">

                        Branch:
                        {" "}
                        {c.branch}

                      </div>

                      <div className="mt-2 text-pink-100/50 text-sm">

                        Expected Cutoff:
                        {" "}
                        {c.cutoff}

                      </div>

                    </div>

                  )
                )}

              </div>

            </div>

            {/* BUTTONS */}

            <div className="grid md:grid-cols-3 gap-5 mt-10">

              <button
                onClick={() =>
                  setPage("marks")
                }
                className="p-5 rounded-[28px] bg-gradient-to-r from-pink-300 to-violet-300 text-black font-black"
              >

                Re-enter Marks

              </button>

              <button
                onClick={() =>
                  setPage("shift")
                }
                className="p-5 rounded-[28px] bg-white/10 border border-white/10"
              >

                Change Shift

              </button>

              <button
                onClick={() =>
                  navigator.share?.({
                    title:
                      "RankPulse AI",
                    text:
                      "My predicted percentile is " +
                      percentile,
                  })
                }
                className="p-5 rounded-[28px] bg-white/10 border border-white/10"
              >

                Share Result

              </button>

            </div>

          </div>

        )}

        {/* HISTORY */}

        {page === "history" && (

          <div className={glass + " p-10"}>

            <div className="flex items-center gap-3 text-4xl font-black mb-10">

              <History />
              Prediction History

            </div>

            <div className="space-y-5">

              {history.map(
                (
                  h,
                  i
                ) => (

                  <div
                    key={i}
                    className="p-6 rounded-[28px] border border-white/10 bg-gradient-to-br from-pink-300/10 to-violet-300/10"
                  >

                    <div className="flex justify-between">

                      <div>

                        <div className="text-2xl font-black">

                          {
                            h.percentile
                          }
                          %

                        </div>

                        <div className="text-pink-100/50 text-sm mt-1">

                          {h.shift}

                        </div>

                      </div>

                      <div className="text-right">

                        <div className="text-xl">

                          {
                            h.marks
                          }
                          /200

                        </div>

                        <div className="text-pink-100/40 text-sm">

                          {h.time}

                        </div>

                      </div>

                    </div>

                  </div>

                )
              )}

              {history.length ===
                0 && (

                <div className="text-pink-100/50">

                  No previous predictions yet.

                </div>

              )}

            </div>

          </div>

        )}

      </div>

    </div>
  );
}